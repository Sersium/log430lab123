# Dockerfile for POS application
FROM python:3.9-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY src/ src/
# Default command prints CLI help
ENTRYPOINT ["python", "-m", "src.cli"]
